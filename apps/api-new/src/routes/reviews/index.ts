import type { FastifyInstance } from "fastify";

export async function reviewRoutes(_app: FastifyInstance) {
  // Review endpoints are exposed through the legacy-compatible
  // collection and flashcard route groups.
}
